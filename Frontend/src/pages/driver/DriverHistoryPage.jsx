import { BusFront, CalendarDays, CheckCircle2, Clock3, Route } from 'lucide-react';
import { StaffPageHeading } from '../../components/staff/StaffUI';
import { driverTripHistory } from '../../services/operationsData';
export function DriverHistoryPage() {
    return <div><StaffPageHeading eyebrow="Your records" title="Trip history" description="Review your recently assigned and completed services."/><div className="staff-kpi-row"><div><BusFront /><span><small>Trips this week</small><strong>8</strong></span></div><div><Clock3 /><span><small>Driving time</small><strong>13h 42m</strong></span></div><div><CheckCircle2 /><span><small>Completed safely</small><strong>7</strong></span></div></div><section className="staff-history-card"><div className="staff-history-header"><span>Trip</span><span>Schedule</span><span>Duration</span><span>Status</span></div>{driverTripHistory.map((trip) => <article key={trip.id}><div><span className="staff-square-icon staff-square-icon--small"><Route /></span><span><small>{trip.id} · {trip.date}</small><strong>{trip.routeCode} · {trip.routeName}</strong><em>Bus {trip.busNumber}</em></span></div><div><CalendarDays /><span><small>Started</small><strong>{trip.startTime}</strong><em>Ended {trip.endTime}</em></span></div><strong>{trip.duration}</strong><span className={`staff-record-status staff-record-status--${trip.status}`}>{trip.status}</span></article>)}</section></div>;
}
